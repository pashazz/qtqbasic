#include "worker.h"
#include <QtGui>

Worker::Worker(QString op, QString args)

{

}
/*QRectF Worker::int2float(QRect *rect) {
 int x, y, w, h;
    x = rect->x;
    y = rect->y;
    w = rect->width();
    h = rect->height();
    QRectF r;
    r.setWidth(w);
}
*/
